name = "persona"
compatibility_date = "2024-02-06"

[pages_build_output]
dir = "./build"
# This is the modern flag that tells Cloudflare to handle SPA routing.
# All requests that don't match a static file will serve index.html.
single_page_application = true
